%% -*- erlang -*-
%%
%% This file is intentionally empty to reflect that under normal
%% operation, it does not need to be modified. Place your local
%% changes, if needed, here.
%%
[
  "config/sys.config",
  %% Example, empty override stanza, required for syntax.
  {blockchain,
    [
    ]}
].
