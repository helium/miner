%% -*- erlang -*-
[
  "config/sys.config",
  {lager,
    [
      {log_root, "/var/data/log"}
    ]},
  {blockchain,
    [
      {key, ${KEY_CONFIG}} %% {gateway_ecc, [{key_slot, 0}]}
    ]},
  {miner,
    [
     {gateway_and_mux_enable, ${GW_AND_MUX}},
     {jsonrpc_ip, {0,0,0,0}}, %% bind jsonrpc to host when in docker container
     {radio_device, { {0,0,0,0}, 1681,
        {0,0,0,0}, 31341} },
     {use_ebus, false}
    ]}
].
