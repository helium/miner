sudo: false

language: erlang

otp_release:
  - 20.1

cache:
  directories:
  - $HOME/.cache/rebar3

## script: ./rebar3 as test do ct --verbose && ./rebar3 dialyzer && ./rebar3 coveralls send
## script: ./rebar3 as test do ct --verbose && ./rebar3 coveralls send
script: ./rebar3 as test do ct --verbose
